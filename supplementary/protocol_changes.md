# protocol_changes.md — audit item → change (track "Протокол", brainsci-4583950 revision 1)

Files changed in this track: `Supplementary_Protocol_S5.md` (protocol v2), `section9_v5.md` (full §9 replacement; §9.1 = 999 words), `S4_rows_P32_P34.csv` (three new register rows), `protocol_numbers_v3.py` → `protocol_numbers.json` (source of every new number). Files NOT touched: the response to reviewers (E5), `section9_power.py`, `trials_budget.csv`, `Table_S4_v2.csv` itself, abstract, README.

| Audit item (gpt_audit_v4.txt) | Finding | Change | Where |
|---|---|---|---|
| §3 | C0 compares 1/2/3 *compatible* responses at zero shared effectors and measures visual access; P16 concerns *incompatible* skeletomotor inclinations and experienced conflict. Not a test of P16. Silence beyond two ≠ stated equivalence. | C0 relabelled as a test of **P32** (author-derived: threshold of access at ≥ 2 co-executable policies); C0-eq relabelled as a bounded estimate of **P33** (author-derived: no further increase 2→3; source silent, silence is not a null). Both rows added to the register with `status=derived`, `published_author_statement='no (author-derived)'`, `explicit_endorsement_of_this_test='no'`, `distinguishable_from` empty. "Tests a stated threshold" replaced everywhere in S5 and §9 by "tests two author-derived operationalisations (P32, P23) of one account's stated commitments and estimates a third quantity"; S5.2 and §9.1 state plainly that the condition tests no stated prediction of any theory directly and discriminates between no two theories. P16 kept in the S5.2 table as **not tested**, with the reason. P23 remains labelled as the authors' derivation. | S5 header, S5.2, S5.12, S5.13; §9.1 "What is tested", "Contrasts"; §9.2 sentence; closing paragraph (two sentences); `S4_rows_P32_P34.csv` |
| §4 | 102 presented per cell pooled two sessions; session 2 alone gave 51 × 0.75 × 0.85 = 32.5 usable no-report trials, not 65. | Budget split by session. Session 2 (confirmatory, no rating): 104 presented per presentation cell = 26 probe + 78 no-probe; expected usable 78 × 0.85 = 66.3; 11 cells → 1144 presented; 22 blocks × 52 trials (13 probe each); 114.4 min of trials at 6 s + 21 × 1 min breaks = 135.4 min. Session 1 (titration, PAS on every trial, classifier training): 64 presented per cell (design choice; probe quarter exact at 32-trial blocks), 704 trials, 598.4 expected training trials after loss, 93.9 min at 8 s + breaks = 114.9 min. Minimum session-2 presentation for E ≥ 64 confirmed as 101. §9.1 "102 presented … 561 per session … about 65 usable" sentence replaced. The pooled-cell mixed-model check (64 usable) is noted as conservative for pooled cells. A permitted variant (split session 2 into two visits) is named for preregistration if the pilot shows 135 min is unsustainable. Study 2 budget corrected in the same way (936 / 576 presented; 110.6 / 93.8 min). | S5.7, S5.8, S5.15, S5.16; §9.1 "Trial budget"; `protocol_numbers_v3.py` |
| §10 (a) blocks | Mapping "fixed per block" but cells "intermixed in blocks". | Presentation cells are blocked, not interleaved: 11 cells × 2 blocks per session; block cue (lever diagram, 5 s) at block start; Latin-square order counterbalanced across participants, reversed between the two blocks of a cell; 4 unanalysed practice trials per block in session 1. Time-in-session confound acknowledged. | S5.5, S5.6; §9.1 "Design" |
| §10 (b) probe | Probe demands one named response; pilot criterion demanded joint execution. | Probe names ONE rule, executed alone, on a quarter of trials; joint execution never required. Pilot criterion 3 rewritten to accuracy on the probed rule. Open question stated: simultaneous incompatible intentions vs rules merely held until the probe; manipulation check named: probe-response latency by shared-pairs level at count 3, reported with interval, not tested; a null weakens the intention reading and is reported as such. | S5.7, S5.14 (criterion 3); §9.1 "Design", "Mandatory pilot" |
| §10 (c) EEG index | Channel set, preprocessing, classifier family deferred; PAS binarisation, inner tuning, leakage protection undefined. | Specified, all labelled "to be preregistered": 64-channel extended 10–20 montage; 17 named parieto-occipital channels; preprocessing pipeline (bad-channel interpolation, ICA ocular/cardiac removal, −200–800 ms epochs, ±100 µV rejection, 100 Hz); features = twenty 20-ms bins 100–500 ms × 17 channels; shrinkage-regularised LDA, no other family; PAS binarised 1 vs 2–4, fixed; nested CV with block-wise outer folds (22 session-1 blocks), inner shrinkage selection; stimulus identity balanced across folds; freezing = weights + hyperparameters hashed to archive after session 1 before any session-2 trial is scored. Window changed from 100–350 ms (v1) to 100–500 ms per the binding decision; the statement that the P3b cannot be excluded entirely is made explicit. | S5.10; §9.1 "Access index" |
| §10 (d) pilot criterion 2 | "Reproduce rating-based cell differences" selects participants with the desired effect; AUC threshold creates a selected population. | Criterion 2 now tests measurement validity only: transfer AUC ≥ 0.70 at each other incompatibility level; reproducing cell differences removed as a criterion (reported descriptively). Selected population named: adults in whom awareness of a masked face is decodable from posterior EEG at AUC ≥ 0.70; exclusions on this criterion reported as k of n; conclusion restricted to that population. | S5.9, S5.10, S5.14 (criterion 2); §9.1 "Mandatory pilot" |
| §10 (e) autonomic windows | ITI, timing markers and overlapping-response model missing. | ITI 2.5–3.5 s jittered; markers at fixation, target, probe, response, ITI onset; pupil window 0.5–2.5 s with GLM deconvolution; SCR 1–5 s via continuous decomposition analysis (overlapping responses); HEP unchanged. | S5.7, S5.11; §9.1 "Autonomic recording" |
| §10 (f) multiplicity | Two confirmatory contrasts at α = .05 plus EEG–PAS agreement; 90 % power refers to one paired contrast. | Family = {C0 (P32), C1a (P23)}, Holm at family-wise α = 0.05 [Holm 1979 added as ref 19]. C0-eq = bounded estimate (P33), C1b exploratory — both outside the family. Success rule: EEG-index and PAS-report versions both significant after correction with the same sign. Power stated per contrast singly (0.9032 uncorrected; ≥ 0.8438 at the Bonferroni floor, exact; 0.8939 under Holm assuming independent p-values, labelled an approximation because C0 and C1a share a cell) and for the conjunctive rule as the product under independence (0.712 / 0.799), with the statement that positive dependence would raise it by an amount "[to be computed from the pilot correlation]". N for 90 %: 141 (Bonferroni, single) and 170 (conjunctive, independence). Fixed N = 120 declared not adequately powered for the conjunctive rule under independence. | S5.12, S5.13; §9.1 "Contrasts, multiplicity and power" |
| §10 Study 2 | P13 concerns felt valence and free-energy change, not sad vs happy access; pupil/SCR as covariates unjustified. | Study 2 hypothesis relabelled **P34** (author-derived from P13; three auxiliary assumptions listed in the register row). Pupil/SCR = manipulation checks for arousal matching; excluded from the primary analysis because they may be consequences of the manipulation; covariates only in a sensitivity analysis. Study 2 power stated singly (0.9008, one confirmatory contrast, no correction needed) and under the conjunctive rule (0.8114 under independence). | S5.15; §9.1 "Valence"; `S4_rows_P32_P34.csv` |
| §11 E5/C1b | Response E5 calls C1b confirmatory; S5 and manuscript call it exploratory. | C1b is exploratory in every occurrence in S5 v2 and §9 v5 (S5.12 table, S5.13, §9.1). **The response-to-reviewers file is outside this track and still needs the E5 wording changed and its "Done" status withdrawn** — flagged for the response track. | S5.12, S5.13; §9.1 |
| §11 tense | "deposited", "preregistered", "frozen specification deposited" used before the actions occurred. | Replaced with "prepared for deposit", "to be preregistered", "specification to be frozen" / "to be frozen after session 1". Header of S5 states that nothing has yet been preregistered or deposited; DOI remains a placeholder marked "not yet deposited". §9.1 "fixed and deposited" → "to be preregistered and frozen before session 2 is scored". | S5 header, S5.2, S5.6, S5.9, S5.10, S5.12, S5.14, S5.16; §9.1 |

## Register rows (S4_rows_P32_P34.csv)

Columns follow `Table_S4_v2.csv` with `endorsed_by_proponents` replaced by `published_author_statement` and `explicit_endorsement_of_this_test` (inserted after `test_status`). Ids P32–P34 were assigned by the parent track because P26–P31 are already occupied (P26 = FM×M4a, P27 = PP×M4b, P28 = HOSS×M7). P34 (Study 2 valence hypothesis) is included in the same file for convenience; drop it if the register track prefers a separate file.

## Numbers that are NOT re-derived here

Power figures for single uncorrected contrasts, TOST power, C1b power and the mixed-model check are read from `power_study1.csv`, `power_study2.csv` and `power_mixed_check.csv` (section9_power.py v2.0) exactly as in protocol v1; none was changed. `trials_budget.csv` from section9_power.py v2.0 still carries the pooled two-session budget and is now marked superseded in S5.16; correcting `section9_power.py` (USABLE_TARGET / N_SESSIONS logic, and the audit §5 skipped-simulation issue) belongs to the code track.

## Items still open after this track

1. Session 2 at 135.4 min (plus set-up) is long for a single EEG visit; the split-visit variant in S5.8 must be decided before preregistration.
2. Conjunctive power under realistic EEG–PAS dependence: `[to be computed from the pilot correlation]` — no number has been invented.
3. Pilot sample size, pilot session-2 length and the k-of-n pass rule for criterion 2 remain `[NUMBER: …]` placeholders.
4. Study 2 N = 119 is not a multiple of the six effector rotations that fixed Study 1 at N = 120; the inconsistency pre-dates this track and was left for the power track.
5. Percent signs remain in S5 and §9 only for statistical parameters (90 % power, 15 % loss, 35–65 % visibility band, 95 % interval), as in protocol v1; no proportion of a count is written as a percentage.
