# CODE_CHANGES.md — track "Код v3" (audit gpt_audit_v4, sections 4–7)

Manuscript brainsci-4583950, revision 1. Every script below carries `__version__ = "2.1"`. CLI compatibility is kept (new options are additive). Every number in this file was read from the outputs of the scripts as run on 24 September 2026 (`reproduce_report.json`, `section9_key_numbers.json`, `Table_2_counts_summary.json`, `adversarial_results.json`); none is typed from memory.

Files changed or added: `agreement.py`, `reproduce.py`, `section9_power.py`, `s2_sensitivity.py`, `build_table2.py` (new). Outputs regenerated: `trials_budget.csv`, `power_mixed_check.csv` (200 simulations per contrast), `Table_2_counts.csv`, `s2_sensitivity.csv`, `expected_hashes.json`, `reproduce_report.json`. No coding value in `Table_S2_v2.csv`, `Table_S1_v2.csv` or `Table_S3_*.csv` was changed.

## Audit item → fix → test

| # | Audit item (section) | Defect as reproduced | Fix (file) | Test that now catches it |
|---|---|---|---|---|
| 1 | §6: 15 rows beginning "none named (…)" gave `by_authors_flag=True` | `load_s2_v2` excluded only the exact word `none` | `agreement.py`: structured parser `parse_attribution`. A value is *no attribution* when empty or matching `^(none\|not named\|no theory\|n/a\|-\|not verifiable)\b` (case-insensitive; `not verifiable` added because 9 rows of the frozen table use it and the coder's own `by_authors_named` flag reads them as False — the parser is cross-checked against that column and aborts on a contradiction) | self-test `negative (v2.1): none-named attribution flagged True`; `adversarial_checks.py` → `no_theory_text_misclassified` now `[]` (was 15 rows) |
| 2 | §6: replacing P01's attribution by `SIT` left agreement at 42 of 42 | only a non-empty-text flag was compared | `agreement.py`: theory-id **sets** are compared. Preferred source: new optional column `theory_attribution_by_authors_ids` (semicolon-separated `theory_id`s, validated against `accounts_manifest.csv`; a value that contradicts a "none" text aborts). Fallback: ids extracted from the free text by matching manifest names / ids (short ids match case-sensitively so that "sit"/"hot" in prose are not theories). Reported separately: binary raw agreement + κ, set raw agreement + κ, per-coder list of unparseable rows. The same parser is applied to `theory_attribution_later` when present (extra) | self-test `negative (v2.1): wrong theory id undetected` (binary 6 of 6, set 5 of 6, row in disagreements); adversarial `wrong_named_theory_detected: true` (binary metric still 42 of 42, as it should be — the set metric is the one that sees it) |
| 3 | §6: `modality=BANANA` accepted | only non-emptiness was checked | `agreement.py`: strict vocabularies — modality ∈ {visual, auditory, tactile, motor, interoceptive, mixed, not applicable}; affective_status ∈ {neutral, valenced, interoceptive}; report_type ∈ {report, no-report, both}; contested_inclusion ∈ {True, False}; a trailing parenthetical qualifier is stripped ("both (targets reported; …)" → both). Any other value aborts naming row and value | self-test `negative (v2.1): invalid modality accepted (BANANA)` and `contested_inclusion outside {True, False}`; adversarial `invalid_modality_rejected: true` |
| 4 | §6: two identically truncated 2-row S1 files accepted | only the two files' key sets were compared with each other | `agreement.py`: `check_manifest_grid` — each file must hold exactly the accounts × domains grid (12 × 10 = 120 keys); missing or extra keys abort (`--allow-partial-grid` downgrades to a loud warning for the 88-cell v1.3 comparison only). S2 v2: both files must hold the experiment_id set of `--s2-reference` (default coder-1 file); the count is reported (`S2v2_n_reference_experiment_ids`) | self-tests `negative (v2.1): truncated S1 accepted (both files 2 rows)`, `negative: mismatched row sets` (now aborts on the grid check), `negative (v2.1): S2 id set mismatch accepted`; adversarial `same_incomplete_S1_rejected: true` |
| 5 | §6: separate handling of UNRESOLVED | UNRESOLVED was a code label but was not reported per coder; S2 fields had no such category | `agreement.py`: UNRESOLVED is its own label in every κ (S1 code, S1 code × polarity, every S2 v2 field); `n_UNRESOLVED_coder1/2` reported for S1 and for each S2 field. The frozen table's `report_type = "not verified (…)"` (P08-E3) is read as UNRESOLVED through a documented alias and the affected rows are listed (`S2v2_report_type_rows_read_as_UNRESOLVED_via_alias`) | self-test `positive (v2.1): S2 v2 pipeline, identical files` asserts `n_UNRESOLVED_coder1 == 1` on the synthetic table |
| 6 | §5: `reproduce.py` reported PASS and "200 mixed-model simulations" although statsmodels was missing and the file held `contrast=skipped, n_sims=0`; the reference hash matched that skipped row | note was written from the request, not from the output; hash of a skipped row was the reference | `reproduce.py`: without `--skip-mixed`, statsmodels must be importable or step 4 FAILs with "statsmodels missing; pass --skip-mixed to run without the mixed-model check". After the run `power/power_mixed_check.csv` is read back: both C0 and C1a must be present with `n_sims == --mixed-sims` and numeric `n_nonconverged`; the note reports the actual n_sims, mixed power and statsmodels version. `section9_power.py` itself now exits non-zero in the same situation and writes the `skipped` row only when `--skip-mixed` is given. `power_mixed_check.csv` stays in the STOCHASTIC (WARN-only) hash list, but its content is checked. The deposited `power_mixed_check.csv` and `expected_hashes.json` now come from a real 200-simulation run (statsmodels 0.14.6) | run with statsmodels hidden → `[FAIL] 4 section9_power -- statsmodels missing; …`; `check_mixed_csv` on a 3-simulation file with 200 requested → FAIL "n_sims = 3, requested 200"; on a one-contrast file → FAIL "C0 … missing"; on a skipped row without `--skip-mixed` → FAIL |
| 7 | §7: `s2_sensitivity.csv`, Table 2 and S3 were not regenerated or hash-checked; "every count" was wider than the check | steps absent from `reproduce.py` | `reproduce.py`: step 1c `build_table2.py` → `table2/Table_2_counts.csv` (EXACT hash); step 1d `s2_sensitivity.py` → `s2/s2_sensitivity.csv` (EXACT hash); step 1e recomputes `Table_S3_tallies.csv` by groupby(dimension_id, code) over `Table_S3_contrast_dimensions.csv`, checks that each theory codes every dimension exactly once, FAILs on any mismatch. `reproduce_report.json` now records per-step parameters (inputs, n_sims per contrast, statsmodels version, mixed power, Table 2 baselines, S3 totals) | full run: 9 of 9 steps PASS; S3: 12 dimensions × 3 codes from 120 cells (10 theories × 12 dimensions) match; YES 54, IMPLICIT 46, NO 20 |
| 8 | §7: 42 rows mix units (experiment / experiment-group / publication-as-one) | one denominator only | `build_table2.py` (new): Table 2 at both units from `Table_S2_v2.csv` (column `unit_type`). Publication level: one row per `publication_id`; theory_addressed = any row True; class = the single class or `motor-conflict (skeletal + autonomic effector)` when both motor classes occur (any other mixture is printed and coded `mixed` — none occurs); baseline = theory-addressed, content-bearing, Whalen (P29) excluded → **21 publications**: neutral-visual 15 of 21, neutral-nonvisual 1 of 21, motor-conflict (skeletal) 3 of 21, motor-conflict (skeletal + autonomic effector) 1 of 21, interoceptive 1 of 21, valenced 0 of 21. Experiment level as before → 30 rows: neutral-visual 18 of 30, neutral-nonvisual 3 of 30, motor-conflict (skeletal) 7 of 30, motor-conflict (autonomic effector) 1 of 30, interoceptive 1 of 30, valenced 0 of 30; unit heterogeneity of that denominator: experiment 18 of 30, experiment-group 2 of 30, publication-as-one 10 of 30 (whole table: 22, 2, 18 of 42). `s2_sensitivity.py`: every scenario is also evaluated at publication level (`unit=publication`), both baselines are printed; experiment rows carry the unit-type counts | `build_table2.py --expect-publications 21` passes; publication-level S0 in `s2_sensitivity.csv` equals the `build_table2.py` baseline (15 / 1 / 4 (3 + 1) / 0 / 1 of 21); hash-checked in `reproduce.py` |
| 9 | §4: the usable-trial budget pooled both sessions (102 × 0.75 × 0.85 = 65.0), but only session 2 supplies no-report trials (51 × 0.75 × 0.85 = 32.5) | `presented_per_cell` divided across two sessions | `section9_power.py`: `presented_session2_per_cell` = smallest integer with exactly one quarter probe trials whose no-probe trials survive the 15 % loss at ≥ 64: **104 = 26 probe + 78 no-probe; 78 × 0.85 = 66.3** (raw minimum 100.39). Session 1 presents the same 104 per cell (equal sessions; `--session1-per-cell` overrides). 11 presentation cells → **1144 presented in session 2**, 2288 over both sessions. Duration at 6 s per trial in 11 blocks of 104 with 2-min breaks: **134.4 min per session** (`--trial-seconds`, `--block-trials`, `--break-minutes` are planning values). Analysis cells pooling three presentation cells: count1@0, count2@0, count3@1 → 198.9 expected usable session-2 trials; count3@0, count3@3 → 66.3. Study 2: 9 × 104 = 936 session-2 trials, 109.6 min. The mixed check stays calibrated at the 64-usable floor and says so (`calibration_note`). Key numbers added: `presented_session2_per_cell`, `usable_session2_per_cell_expected`, `presented_session2_total`, `session2_minutes` (+ probe/no-probe counts, per-analysis-cell pooled counts, the v2.0 pooled value 102 for comparison) | `trials_budget.csv` regenerated and EXACT-hashed; assertion inside `presented_session2_per_cell` (expected usable ≥ target, probe count exact) |

## Mixed-model check, reference run (200 simulations per contrast, seed 20260924/20260925, N = 120, 64 usable trials per analysis cell, dz = 0.30, statsmodels 0.14.6)

| contrast | n_sims | analytic | two-stage paired t | linear mixed model | non-converged |
|---|---|---|---|---|---|
| C0_threshold_count1_vs_ge2 | 200 | 0.903 | 0.900 | 0.900 | 0 |
| C1a_compatible_vs_incompatible_at_count3 | 200 | 0.903 | 0.935 | 0.935 | 0 |

Monte Carlo SE at 200 simulations ≈ 0.021, so these values and the 0.930 / 0.920 quoted in Protocol S5 are within sampling error of each other and of the analytic 0.903; the protocol text should be updated to the deposited run.

## Self-tests (agreement.py --selftest): 24 of 24 cases pass

Negative cases required by the task, all caught: none-named attribution flagged True; wrong theory id undetected; invalid modality accepted; truncated S1 accepted; S2 id set mismatch accepted. Extra: ids column with a theory_id not in the manifest; contested_inclusion outside {True, False}. Pre-existing 8 negative cases retained. The self-test log is now path-independent (output directory replaced by `<out>/`) so that its EXACT hash is stable.

## adversarial_checks.py against the fixed agreement.py

`no_theory_text_misclassified: []` · `invalid_modality_rejected: true` · `wrong_named_theory_detected: true` (binary `wrong_theory_attribution_agreement` stays "42 of 42" by design; the set-level metric reports 41 of 42) · `same_incomplete_S1_rejected: true`. The remaining entries of that JSON (codebook hashes, S4 identity between deposit and blind pack, M8 pair confidence, the two trial arithmetic lines) concern files outside this track and were evaluated on a reconstructed layout (deposit_v2 codebook beside the audited coder_pack_v3.zip), so they are not evidence either way here.

## Reproduce chain (reproduce.py v2.1)

Steps 1, 1b, 1c, 1d, 1e, 2, 3, 4, 5 — 9 of 9 PASS on the full reference run (62.9 s); `expected_hashes.json` rewritten from that run (14 files: 12 EXACT, 2 STOCHASTIC). Verification runs completed after the reference run, read from their `reproduce_report.json` / stdout: a second full run with `--mixed-sims 200` gives 9 of 9 PASS and "14 of 14 files match" (all 14 sha256 values identical to the reference, including the seeded `power_mixed_check.csv`); two `--skip-mixed` runs give 9 of 9 PASS and "13 of 14 files match" with the single WARN on `power_mixed_check.csv` (the skipped row; step 4 reports the skip explicitly). Note: `recompute/table1_tallies_legacy.csv` is only produced when step 1 runs on the legacy v1.3 file and is no longer demanded on a v2 run (the bundled v2.0 reference expected it and therefore always FAILed on `Table_S1_v2.csv`).

## Open points for the authors (not code)

- Session length of about 134 min at the 6 s / 2-min-break planning values exceeds two hours; either accept, shorten the per-trial time, or split into more sessions (the script takes the timing parameters on the command line).
- `report_type = "not verified (…)"` for P08-E3 is read as UNRESOLVED; the codebook should either adopt UNRESOLVED as the spelling or the row should be recoded by the authors (not changed here).
- `theory_attribution_by_authors_ids` does not yet exist in `Table_S2_v2.csv`; the free-text extraction reproduces the coder's `by_authors_named` flag on all 42 rows, but the structured column is the intended input for the second coder.
